subprojects {
    repositories {
        jcenter()
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.10'
}

ext {
    buildToolsVersion = '23.0.2'
    compileSdkVersion = 23

    minSdkVersion = 4
    targetSdkVersion = 19
}